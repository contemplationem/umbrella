# ensure SSH is installed
sudo apt install ssh

# enable root login over SSH
awk '{ gsub ("#PermitRootLogin prohibit-password", "PermitRootLogin yes", $0); print}'

# restart SSH
service ssh restart

# paste status of SSH
sudo systemctl status sshd

quit
