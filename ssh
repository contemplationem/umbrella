# ensure SSH is installed
sudo apt install ssh

# enable root login over SSH
sudo sed -i 's/#PermitRootLogin[[:space:]]prohibit-password/PermitRootLogin[[:space:]]yes/' /etc/ssh/ssh_config

# restart SSH
service ssh restart

# paste status of SSH
sudo systemctl status sshd
